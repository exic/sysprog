test: $(OBJS)
	[ -d tests ] && cd tests && make || true

clean:
	rm -f $(OBJS) $(OUT)
	[ -d tests ] && ( cd tests && make clean ) || true

%.o: %.cpp
	echo CC $<
	${CXX} -c ${CXXFLAGS} $<

# vim: set filetype=make : #
